# 💭 2026-01-19 (Mon) TIL

## 🌱 Todo

### 코테 준비
- □ **solved.ac class 2** 5문제 
  - ☑️ 11726, 11727, 1764, 11399
  - ✍️ 1463
      
  
- **자료구조** 
  - DP

- ✔️ ~~새싹(SeSAC) 강의 진도 따라가기~~
  - 비동기 클라이언트
  - session
  

...

## 🎧 간단 정리

### 알고리즘
> **동적 계획법(DP)**
- `Dynamic Programming` 
    - 수많은 경우의 수를 모두 고려해야하는 문제의 수행시간 단축
    - 최단 경로, 최고 점수
    - 최적의 값 갱신
    - 각위치의 최대의 합
- 💡 **전략!**
  1. 최댓값, 최솟값, 방법의 수를 묻는 문제중 완전탐색은 시간이 오래걸릴것 같다. -> `dp 의심`
  2. 상태 정의 dp[ i ] 인덱스가 무엇을 의미하는지 정의
       -  예: `dp[i]` = i번째 계단까지 올랐을 때의 최대 점수
  3. `점화식` 세우기 : 이전 단계의 값을 이용해 현재값 어떻게 구할지
  4. `기초값` 설정
  5. `구현`: 반복문(Bottom-Up)이나 재귀(Top-Down)를 사용

> **정렬**
- 리스트 내부의 리스트
  - 특정 인덱스로 정렬
    ```python
    # 1번 인덱스(10, 5, 8)를 기준으로 정렬하고 싶을 때
    data = [[1, 10], [3, 5], [2, 8]]

    # 방법 1: 일반 함수 사용
    def func(el):
        return el[1]
    sorted_data = sorted(data, key=func)

    # 방법 2: 람다 사용 (가장 많이 씀)
    sorted_data = sorted(data, key=lambda el: el[1])
    # 결과: [[3, 5], [2, 8], [1, 10]]
    ```
- `set`
  - 합집합 교집합 연산은 리스트보다 set이 훨씬 빠름

    ```python
    movies.sort(key=lambda x: x['vote_average'])
    pprint([(m['title'],m['vote_average']) for m in movies])
    ```

- `딕셔너리 정렬`
  ```python
  movies = [
      {'title': 'A', 'score': 9.5},
      {'title': 'B', 'score': 8.8}
  ]
  # key에 들어가는 함수는 데이터 정렬 기준!
  # 점수(score) 기준으로 정렬
  sorted(movies, key=lambda m: m['score'])
  ```

...

## 🐚 회고
**집에서 노트북 여는게 너무 힘들어.. 공부할수 있는 환경을 만들자**