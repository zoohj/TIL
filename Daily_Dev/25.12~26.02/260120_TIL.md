# 💭 2026-01-20 (Tue) TIL

## 🌱 Todo

### 코테 준비
- □ **solved.ac class 3** 2문제 
  - ☑️ 
  - ✍️ 2606, 1874

- ✔️ ~~새싹(SeSAC) 강의 진도 따라가기~~
  - 공공데이터포털 api 연결 연습
  - async

...

## 🎧 간단 정리

### 비동기 프로그래밍
```python
await asycio.sleep(2) 
# 2초동안 할일 없으니까 그동안 다른 작업 처리 
# -> 이벤트 루프에 제어권 넘김
```
1. `asyncio.gather`(진정한 병렬 처리)
    
    ```python
    results = await asyncio.gather(*(fetch_url(u)for u in urls))
    ```
2. 반복문 안에서 `await` (순차적 실행)
    ```python
    result = []
    for u in urls:
      result = await fetch_url(u)
      results.append(result)
    ```
- 첫번째 요청을 보내고 결과를 받은 후에야 다음 요청 보냄
---
### 이벤트루프의 역할 = 작업 관리자
1. gather로 들어온 작업들을 리스트에 적어둠
2. 1번 작업이 "나 잠깐 쉴게(await)"하면 즉시 다음 작업 실행
3. 그러다가 1번작업이 "나 다쉬었어"라고 신호주면 다시 1번으로 돌아와 남은 코드 실행

    ```python
    async with aiohttp.ClientSession() as session:
        # 1. 태스크 리스트 생성 (아직 실행 전)
        tasks = [fetch_url(session, url) for url in urls]
        
        # 2. gather를 통해 한꺼번에 비동기 실행 및 결과 수집
        responses = await asyncio.gather(*tasks)
    ``` 
...

## 🐚 회고
**gg**