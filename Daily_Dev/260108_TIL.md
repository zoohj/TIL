# 💭 2026-01-08 (Thu) TIL

## 🌱 Todo

### 코테 준비
- □ **solved.ac class 2** 4문제 
  - ☑️ 2869, 10989, 11050, 9012

- ✔️ ~~새싹(SeSAC) 강의 진도 따라가기~~
  - ☑️ 점심 랜덤팀 조짜기 프로그램

...

## 🎧 간단 정리

### 파이썬 기초 개념
> **문법**
- **break**

  `안쪽 루프만 종료, 바깥 로직 흐름은 계속 진행됨`

- **메모리 관점에서 생각하기**

  - 무조건 while로 반복을 돌리면 불필요한 반복이 발생할 수 있음

  - 문제를 풀 때는 \
→ `얼마나 많은 데이터를, 어떤 형태로, 한 번에 처리하는지` 고려해야 함

    예시:

    ```python
    count = [0] * 100001
    ```

    → 입력 범위가 명확할 때는 이런 방식이 메모리·시간 모두 효율적

- **‘사람 생각’이 아니라 ‘컴퓨터 생각’**
  - 문자열을 “의미”로 보지 말고 \
→ **문자의 나열, 상태 변화, 숫자 연산**으로 바라보기
  - VPS 문제처럼 \
  → 괄호를 문자 그대로 보지 않고 `+1 / -1` 상태로 치환하는 사고가 중요



...

## 🐚 회고
**class 2 차례대로 다 풀어보려고했는데 간단 노가다 문제는 건너뛰고 실버를 푸는게 낫다고 하셔서 방향 수정했다.** 

**자료구조별로 문제를 풀지, 아니면 난이도별로 풀지 고민했는데 이미 자료구조를 조금 아는 상태이기 때문에 문제를 먼저 확인하고 어떤 방식으로 해결할지 고민하는게 좋을 것 같다고 하심**



### 🤖 점심 랜덤 팀 조짜기 프로그램 회고

수업 중에 점심 랜덤 팀 조짜기 프로그램을 구현했다.  
기능 구현 자체는 완료했지만, **개선 여지**가 명확히 보였다.

---

#### 현재 로직에서의 사고 과정

- 팀 인원은 **3~5명**이 적당하다고 판단
- 우선 **4명 기준으로 팀을 분할**
- 나머지 인원 처리 방식
  - **3명이 남으면** → 팀 하나 추가
  - **2명 이하가 남으면** → 기존 4명 팀에 한 명씩 분배

---

#### 다시 생각해보니 든 의문

- 실제 식당 환경을 고려하면 **4인 테이블이 가장 많음**
- 그렇다면:
  - **“최대 4명” 기준으로 바꾸는 것이 합리적**
  - 하지만 기준을 3명으로 바꾸면
    → 오히려 **3명 팀이 과도하게 많아질 가능성**이 있음 \
    -> ⭐️ **개선 필요**

---

#### 현재 코드 기준에서의 고민 포인트

- **팀 최대 인원을 4명으로 변경하고**
- **3명 팀의 비율을 줄이기 위해**
- 재설계 필요
  - `mem_count`
  - `team_count`
  - 잔여 인원 분배 로직
  
  - +@) 결석자 확인(yes/no)에서 잘못된 값 처리

---

**문제를 코드로 옮긴 뒤 실제 조건을 다시 반영하며 구조를 수정해 나가는 사고 과정 자체가 중요**


<details>
<summary>Python 코드 보기</summary>

```python
'''
점심 랜덤 조짜기 프로그램

조건
1. 팀별 균일한 인원수(3명 이상)
2. 팀별 인덱스 지정
3. 결석한 사람 체크 기능

결과값 포맷
result = {team1: ['   ', '    ', '    ']}

''' 

import random
import sys
input = sys.stdin.readline
from datetime import date


def input_absence_student(names):
    print("결석한 사람의 이름을 입력하세요(ex_홍길동 신사임당)")
    absence_names=list(map(str, input().split()))
    
    check_list = []
    for ab_name in absence_names:
        check_list.append(check_name(ab_name, names))
    if not all(check_list):
        return input_absence_student(names)
    return absence_names

def check_name(ab_name, names_list):
    if ab_name in names_list:
        return True
    else: 
        print()
        print("올바른 참여자 이름을 입력하세요")
        print()
        print("[도봉 새싹 참여자 명단]")
        for i in range(0, len(names), 4):
            print(*names[i:i+4])
        print()
        return False
    

names = [
    '강성신', '김수왕', '김아네스', '김진우', '남건우', 
    '박세형', '박지훈', '배은하', '사비카', '서현석', 
    '이상준', '이상호', '이종혁', '이지선', '이채훈', 
    '주현지', '최유리', '하재연', '한다솔', '황희연'
    ]



print("=================================")
print(date.today())
print("점심 랜덤 조짜기 프로그램")
print("=================================")
print()


today_lunch=names

# 결석 확인 로직
absence_names=[]
print("오늘 결석한 사람이 있나요?(yes/no)")
answer= str(input().strip())
if answer == 'yes':
    absence_names=input_absence_student(names)
    today_lunch= list(set(names) - set(absence_names))
print()
print("=================================")
print()

mem_count= 4
team_count = len(today_lunch)//mem_count
team_dic = {}
while len(today_lunch) > mem_count:
    for i in range(1, team_count+1):
        team_name="team"+ str(i)
        member = list(random.sample(today_lunch,mem_count))
        team_dic[team_name]=member
        today_lunch = list(set(today_lunch)- set(member))

team_index=1
while today_lunch:
    if len(today_lunch)==3:
        team_index=team_count+1
        team_dic["team"+str(team_index)]=today_lunch
        break

    else:
        memeber = today_lunch.pop()
        team_dic["team"+str(team_index)].append(memeber)
        if team_index < team_count:
            team_index = team_index + 1 
        else:
            team_index=1

print("오늘의 점심 메이트")
for team, members in team_dic.items():
    print(f"{team}: {members}")

print()
print("=================================")
```
</details> 

<br>